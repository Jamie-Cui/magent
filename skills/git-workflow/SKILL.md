---
name: git-workflow
description: Manage git status, diffs, commits, and pushes while preserving unrelated user changes.
type: instruction
tools: bash
---

# Git Workflow

Inspect `git status` before staging. Stage only intended files, use conventional commit messages, and never revert unrelated changes.

After committing or pushing, confirm the branch state.
